{
  "name": "market4u",
  "version": "2.0.0",
  "description": "Comparador de precios de supermercados en México",
  "private": true,
  "scripts": {
    "build": "rm -rf www && mkdir -p www && cp index.html app.js services.js config.js mockData.js styles.css sw.js manifest.json privacidad.html terminos.html www/ && cp -r icons www/"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/app": "^8.1.0",
    "@capacitor/browser": "^8.0.3",
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0",
    "@supabase/supabase-js": "^2.39.8"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.0"
  }
}
